<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.cx.helloandroid">

    <!-- 地理位置功能 -->
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.CALL_PHONE" />
    <uses-permission android:name="android.permission.CHANGE_WIFI_STATE" />
    <uses-permission android:name="android.permission.INTERNET" />

    <!-- 照相机功能 -->
    <uses-permission android:name="android.permission.VIBRATE" /> <!-- 震动权限 -->
    <uses-permission android:name="android.permission.CAMERA" />

    <uses-feature android:name="android.hardware.camera" /> <!-- 使用照相机权限 -->
    <uses-feature android:name="android.hardware.camera.autofocus" />

    <!-- 自动聚焦权限 -->
    <application
        android:allowBackup="true"
        android:hardwareAccelerated="false"
        android:icon="@mipmap/apple"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/apple"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"> <!-- android 4.0以上的手机，应该禁用硬件加速 -->
        <activity android:name=".MainActivity" />
        <activity android:name=".activity.WelcomeActivity" />
        <activity android:name=".activity.ContentActivity" />
        <activity android:name=".activity.DBActivity" />
        <activity android:name=".activity.MapActivity" />
        <activity android:name="com.google.zxing.activity.CaptureActivity" />
        <activity android:name=".activity.ScanCodeActivity" />
        <activity
            android:name=".activity.StatusActivity"
            android:theme="@style/AppTheme.No" />
        <activity android:name=".activity.AnimationActivity" />
        <activity android:name=".activity.T9Activity" />
        <activity android:name=".activity.SensorActivity" />
        <activity android:name=".activity.BitmapCacheActivity" />
        <activity android:name=".activity.TextViewActivity" />
        <activity android:name=".activity.HelpActivity">

        </activity>
        <activity android:name=".activity.WaterViewActivity" />
        <activity android:name=".activity.PhotoViewActivity" />
        <activity android:name=".webfragment.WebActivity" />
        <activity android:name=".activity.LayoutParamsActivity" />
        <activity android:name=".webfragment.WebTwoActivity" />
        <activity android:name=".activity.CareActivity" />
        <activity android:name=".activity.DrawableActivity" />
        <activity android:name=".activity.ClipActivity">

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>